import 'package:flutter/material.dart';

class AccountDataEdit extends StatelessWidget {
  const AccountDataEdit({Key key}) : super(key: key);
  static const screenName = "/accedit-screen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Hello'),
      ),
    );
  }
}
